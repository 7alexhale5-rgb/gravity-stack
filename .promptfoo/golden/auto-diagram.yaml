- description: "auto-diagram generates valid mermaid"
  vars:
    subject: "Authentication flow for a Next.js app with Clerk"
  assert:
    - type: contains-any
      value: ["flowchart", "sequenceDiagram", "graph", "mermaid"]
    - type: llm-rubric
      value: "Should produce valid Mermaid syntax showing auth flow from user through middleware to Clerk."
